# Bingo App — frontend

Vue 3 + TypeScript + Vite. See the [project README](../README.md) for the
full picture (backend setup, architecture, milestone history).

```bash
npm install
cp .env.example .env.development
npm run dev      # http://localhost:5173, expects the backend on :8000
npm run lint
npm run build
```
